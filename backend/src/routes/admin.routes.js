/**
 * Admin Routes
 * Reserved for future admin-only operations
 * All user management operations have been moved to staff routes
 */

import express from 'express';

const router = express.Router();

// Admin routes are currently empty
// All user management functionality has been moved to /api/v1/staff

export default router;
